#!/bin/bash
#
#       /etc/rc.d/init.d/iprupdated
#
# Runs the iprupdate utility for ipr supported devices
#
# chkconfig:  2345 30 90
# description: Runs the iprupdate utility to update firmware images \
#   for adapters and devices under the ipr device driver.
# processname: iprupdate
. /etc/init.d/functions

prog="iprupdate"

start() {
        echo -n $"Starting $prog: "
        $prog
        RETVAL=$?
        [ $RETVAL -eq 0 ] && touch /var/lock/subsys/iprupdate
        echo
        return $RETVAL
}

stop() {
       return 0
}

rhstatus() {
       status $prog
}

restart() {
       stop
       start
}

reload() {
       return 0
}

case "$1" in
  start)
        start
        ;;
  stop)
        stop
        ;;
  restart)
        restart
        ;;
  reload)
        reload
        ;;
  status)
        rhstatus
        ;;
  condrestart)
        [ -f /var/lock/subsys/iprupdate ] && restart || :
        ;;
  *)
        echo "Usage: $0 {start|stop|status|restart|condrestart}"
        exit 1
esac

exit $?
